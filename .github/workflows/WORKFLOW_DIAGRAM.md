# Build Videos Workflow - Flow Diagram

## Workflow Decision Tree

```
┌─────────────────────────────────────────────────────────────┐
│                    Build Product Videos                      │
│                      Workflow Start                          │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │  Check GCP_SA_KEY    │
                  │  secret available?   │
                  └──────────┬───────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌───────────────┐         ┌─────────────────┐
        │  YES - Full   │         │  NO - Fallback  │
        │     Mode      │         │      Mode       │
        └───────┬───────┘         └────────┬────────┘
                │                          │
                ▼                          ▼
    ┌───────────────────────┐   ┌──────────────────────┐
    │ Prepare GCP           │   │ Skip GCP Setup       │
    │ Credentials           │   │                      │
    └───────┬───────────────┘   └──────────┬───────────┘
            │                              │
            ▼                              ▼
    ┌───────────────────────┐   ┌──────────────────────┐
    │ Authenticate to       │   │ Install FFmpeg       │
    │ Google Cloud          │   │                      │
    └───────┬───────────────┘   └──────────┬───────────┘
            │                              │
            ▼                              │
    ┌───────────────────────┐             │
    │ Get HeyGen API Key    │             │
    │ from Secret Manager   │             │
    └───────┬───────────────┘             │
            │                              │
            └──────────────┬───────────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ Extract ASINs from   │
                │ Google Sheet         │
                │ (or create           │
                │  placeholder)        │
                └──────────┬───────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ Generate Videos      │
                │                      │
                │ • Full Mode:         │
                │   HeyGen AI avatars  │
                │                      │
                │ • Fallback Mode:     │
                │   FFmpeg text videos │
                └──────────┬───────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ Upload Artifacts     │
                │ (product-videos)     │
                └──────────┬───────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │    Workflow End      │
                │      Success ✅      │
                └──────────────────────┘
```

## Mode Comparison

### 🟢 Full Mode Features
```
┌─────────────────────────────────────┐
│          Full Mode                   │
│  (GCP_SA_KEY configured)            │
├─────────────────────────────────────┤
│ ✅ Google Cloud Authentication      │
│ ✅ Secret Manager Access            │
│ ✅ HeyGen AI Video Generation       │
│ ✅ Professional AI Avatars          │
│ ✅ Google Sheets Integration        │
│ ✅ High-Quality Talking Videos      │
└─────────────────────────────────────┘
```

### 🟡 Fallback Mode Features
```
┌─────────────────────────────────────┐
│        Fallback Mode                 │
│  (No GCP_SA_KEY needed)             │
├─────────────────────────────────────┤
│ ✅ No Setup Required                │
│ ✅ FFmpeg Video Generation          │
│ ✅ Text-Based Videos                │
│ ✅ Placeholder ASIN Data            │
│ ✅ Works Immediately                │
│ ⚠️  No AI Avatars                   │
└─────────────────────────────────────┘
```

## Step-by-Step Execution

### Phase 1: Environment Setup
```
1. Checkout repository ──────────► Always runs
2. Setup Node.js ────────────────► Always runs
3. Check GCP availability ───────► Always runs
   │
   └──► Sets: available=true/false
```

### Phase 2: Conditional GCP Setup
```
IF GCP credentials available:
   4. Prepare credentials ──────► Conditional (only if GCP available)
   5. Authenticate GCP ─────────► Conditional (only if GCP available)
   6. Get HeyGen API key ───────► Conditional (only if GCP available)
ELSE:
   Skip steps 4-6
```

### Phase 3: Fallback Installation
```
IF GCP credentials NOT available:
   7. Install FFmpeg ───────────► Conditional (only in fallback mode)
ELSE:
   Skip step 7
```

### Phase 4: Video Generation
```
8. Extract ASINs ────────────────► Always runs
   │                                 (uses credentials if available,
   │                                  creates placeholder if not)
   │
9. Generate videos ──────────────► Always runs
   │                                 • Full mode: Uses HeyGen
   │                                 • Fallback: Uses FFmpeg
   │
10. Upload artifacts ────────────► Always runs
```

## Decision Points

### Is GCP_SA_KEY available?
```
┌─────────────────────────────────────────────────┐
│ Secret Check: GCP_SA_KEY                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  if [ -n "$GCP_SA_KEY" ]; then                 │
│     available=true                              │
│     → Use Full Mode                            │
│  else                                           │
│     available=false                             │
│     → Use Fallback Mode                        │
│  fi                                             │
└─────────────────────────────────────────────────┘
```

### Which video generation method?
```
┌─────────────────────────────────────────────────┐
│ Video Generation Path                           │
├─────────────────────────────────────────────────┤
│                                                 │
│  IF HEYGEN_API_KEY is set:                     │
│     → Use HeyGen AI (professional avatars)     │
│                                                 │
│  ELSE IF FFmpeg is available:                  │
│     → Use FFmpeg (text overlays)               │
│                                                 │
│  ELSE:                                          │
│     → Error (neither available)                │
│                                                 │
└─────────────────────────────────────────────────┘
```

## Error Handling

### Old Behavior (Before Fix)
```
GCP_SA_KEY missing
    │
    ▼
┌──────────────────┐
│  ❌ ERROR        │
│  Workflow fails  │
│  Exit code 1     │
└──────────────────┘
```

### New Behavior (After Fix)
```
GCP_SA_KEY missing
    │
    ▼
┌────────────────────────┐
│  ⚠️  WARNING           │
│  Fallback mode active  │
│  Continue workflow     │
└───────────┬────────────┘
            │
            ▼
┌────────────────────────┐
│  ✅ SUCCESS            │
│  Videos generated      │
│  Using FFmpeg          │
└────────────────────────┘
```

## Triggers

### Automatic (Scheduled)
```
Cron: 0 7 * * 1-5
      │ │ │ │ │
      │ │ │ │ └─── Day of week (1-5 = Mon-Fri)
      │ │ │ └───── Month (any)
      │ │ └─────── Day of month (any)
      │ └───────── Hour (7 = 7 AM UTC)
      └─────────── Minute (0)

Runs: Weekdays at 7:00 AM UTC
```

### Manual (Workflow Dispatch)
```
Via GitHub CLI:
  gh workflow run build-videos.yml

Via GitHub Web UI:
  Actions → Build Product Videos → Run workflow
```

## Outputs

### Artifacts Generated
```
┌────────────────────────────────────┐
│ Artifact: product-videos           │
├────────────────────────────────────┤
│ Path: public/videos/**             │
│                                    │
│ Contents:                          │
│   • NWS_001.mp4                   │
│   • NWS_002.mp4                   │
│   • ...                           │
│   • NWS_012.mp4                   │
│                                    │
│ Format: MP4 (H.264)                │
│ Resolution: 1280x720               │
│ Duration: ~30 seconds each         │
└────────────────────────────────────┘
```

---

**Note:** This diagram represents the workflow logic after the GCP_SA_KEY fix has been applied.
